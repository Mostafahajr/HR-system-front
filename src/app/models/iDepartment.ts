export interface Department {
    id: number;
    department_name: string;
    created_at?: string;  // Optional, for timestamp when department was created
    updated_at?: string;  // Optional, for timestamp when department was updated
  }
  